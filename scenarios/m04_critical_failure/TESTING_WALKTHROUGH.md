# Mission 4: "Critical Failure" — Testing Walkthrough

> Auto-generated by the walkthrough-scenario skill. Last updated: 2026-08-21.
> Reconciled against: dungeon_graph.md (regenerated same day by validate_scenario.rb).
> Setting: Albion Energy Storage — 200 MWh grid-scale lithium-ion BESS.

## Prerequisites

- **Validator passes** — schema OK, 0 room overlaps, 0 ink warnings, critical path resolves (4 hops).
- **Ink compiled** — 10/10 `.ink` → `.json`, runtime loopcheck clean on every entry knot.
- **Minigames on the critical path:**
  - ✅ Person-chat (Vance, guard, Cipher, Relay, Voltage, debrief)
  - ✅ Phone-chat (Agent HaX hub, Vance support hub)
  - ✅ VM launcher + flag-station (BMS jump server, 4 flags)
  - ✅ ESD pushbutton (`interactionType: esd_button`)
  - ⚠️ **VM flags unverified** — `secgen/m04_critical_failure.xml` exists but has not been built, so flag order/count on `bms_jump_server` is inferred. Steps 8–11 cannot be signed off until a SecGen build confirms them.
  - ⚠️ Two field guides (`distcc-exploitation`, `rfid-cloning`) 404 until HacktivityLabSheets is rebuilt (guides are optional, off the critical path).

## Room spine

```
main_entrance (guard)                              [start / reception]
  └─S─ operations_office (Vance → Level 1 keycard)
         ├─S─ security_office              [key]    spare Level 1 in locker
         └─E─ scada_control_room
                └─S─ battery_hall_1 (Cipher → Level 2)
                       ├─(workshop door) engineering_workshop  [rfid: Level 2]  VM + drop-site
                       └─E─ battery_hall_2 (Relay → Master)     [rfid: Level 1]
                              └─S─ plant_room      [rfid: Master]   Voltage + Static + ESD
                                     └─E─ loading_dock [key]        Voltage's escape / go-bag
```

---

## Aim 1 — Get Inside Albion Energy Storage
[Unlocks at start — `status: active`]

1. **Opening briefing (cutscene)** — `opening_briefing_cutscene` plays once (`skipIfGlobal: briefing_played`); the reworked question-hub lets you visit every topic and always exits through `mission_stakes` → the Architect/Social Fabric beat can no longer be skipped → cutscene ends with `-> END`.
2. **Reception** — Player starts in `main_entrance` → **task `enter_facility` completes** (`enter_room: main_entrance`).
3. **Security guard** — Talk past (credentials / bluff) or slip by (raises alarm). Not a gate; guard KO sets `security_guard_ko`. No task tie.
4. **Operations Office — Robert Vance** — Talk to him → ink `#complete_task:meet_robert_vance` → **task `meet_robert_vance` completes**; he hands a **Level 1 keycard** (`#give_item:keycard`). *KO-safety:* `taskOnKO: meet_robert_vance` + spare Level 1 in the security-office locker → a KO'd Vance cannot strand the mission. Completing the aim → **`confirm_the_lie` unlocks** (`aimCompleted: get_inside`).

## Aim 2 — Confirm the Telemetry Is Lying
[Unlocks after: `get_inside` complete]

5. **Operations Office — Maintenance Work Orders** — Pick up → `onPickup` sets `evidence_maintenance_logs_found` → HaX eventMapping **completes `find_infiltration_evidence`**.
6. **Battery Hall 1 — Analog Thermometer** — Read it against the falsified SCADA panel (gated on being in the hall) → interaction sets `anomaly_detected` and **completes `identify_scada_anomalies`**. *This starts both timers and the threat-music cue — nothing before this is on a clock.* Aim complete → **`reach_the_workshop` unlocks**.

## Aim 3 — Reach the Engineering Workshop
[Unlocks after: `confirm_the_lie` complete]

7. **Battery Hall 1 — Cipher** — Neutralise (KO or secure) → `globalVarOnKO: operative_cipher_defeated` → **completes `neutralize_operative_cipher`** and sets `operatives_defeated = 1`. Drops **Workshop Keycard (Level 2)** (`dropNPCItems`, recoverable). *KO-safety:* `taskOnKO: neutralize_operative_cipher`.
8. **Engineering Workshop** — Enter with Level 2 [rfid] → **task `locate_compromised_systems` completes** (`enter_room: engineering_workshop`). Aim complete → **`map_the_attack` unlocks**.

## Aim 4 — Map the Attack on the OT Network
[Unlocks after: `reach_the_workshop` complete]

9. **BMS Jump Server Terminal** — Launch the VM; submit flags at the **SAFETYNET Drop-Site** (`acceptsVms: bms_jump_server`):

| Flag | Source | Task |
|---|---|---|
| flag_1 | falsified rack status page (web) | `submit_network_scan_flag` |
| flag_2 | ProFTPD — OptiGrid transfer | `submit_ftp_intel_flag` |
| flag_3 | HTTP analysis | `submit_http_analysis_flag` |
| flag_4 | distcc CVE-2004-2687 | `submit_distcc_exploit_flag` |

10. On **`submit_distcc_exploit_flag`**, HaX eventMapping sets `attack_mechanism_known`. The three `scan_*` / `investigate_*` / `exploit_*` tasks are **optional** (progress flavour). **Unverified pending a SecGen build.** Aim complete (all four submit tasks) → **`stop_the_runaway` unlocks**.

## Aim 5 — Stop the Thermal Runaway  *(mission conclusion)*
[Unlocks after: `map_the_attack` complete]
`missionConclusion: true` — `requiresCompleted: [identify_scada_anomalies, confront_voltage, disable_attack_vectors]` → `bond_visualiser`.

11. **Battery Hall 2 — Relay** — Neutralise → `globalVarOnKO: operative_relay_defeated` → **completes `neutralize_operative_relay`**, sets `operatives_defeated = 2`. Drops **Master Keycard**. *KO-safety:* `taskOnKO`.
12. **Plant Room** — Enter with Master [rfid]. Static blocks; Voltage at the command laptop.
13. **Confront Voltage** — Optionally push him for the casualty figure. Three stances, **all fire `#complete_task:confront_voltage`** (plus `taskOnKO: confront_voltage`):
    - **Fight** → `#hostile`; KO sets `voltage_captured`.
    - **Arrest** → stands down → ink sets `voltage_captured` (deliberate no-KO exception).
    - **Go for the button** → he escapes → `voltage_escaped`.
    Either `voltage_captured` or `voltage_escaped` → eventMapping sets **`voltage_neutralised`**.
14. **ESD authorisation** — `voltage_neutralised` **AND** `anomaly_detected` → eventMapping sets `esd_authorized` ("The ESD is yours — press it").
15. **ESD Pushbutton** — Press (gated on `esd_authorized`; refused with the two-part reason otherwise) → sets `attack_prevented`, `remote_trigger_disabled`, `mission_complete`; **completes `disable_attack_vectors`**; cancels both timers.
16. **Debrief (cutscene)** — `agent_0x99_debrief` fires on `mission_complete` (hidden-person, `disableClose`). Branches on `racks_vented` / `voltage_captured` / `chen_trust_level` / `robert_vance_ko`. **`#complete_task:report_to_0x99` fires at the END** (so `bond_visualiser` doesn't cover the debrief). Disclosure choice → credits.

**WIN:** `disable_attack_vectors` + `confront_voltage` + `identify_scada_anomalies` all complete → `stop_the_runaway` mission-conclusion satisfied → `bond_visualiser`. Credits title: `MISSION COMPLETE` (no vent) / `COSTLY SUCCESS` (`racks_vented`) / `CATASTROPHIC FAILURE` (`!attack_prevented`, unreachable on a completed run — there is deliberately no `show_end_screen`).

## Global Variable State (end of critical path)

| Global | Set by |
|---|---|
| `briefing_played` | opening cutscene |
| `evidence_maintenance_logs_found` | pick up Maintenance Work Orders (step 5) |
| `anomaly_detected` | read Analog Thermometer (step 6) — **starts timers** |
| `operative_cipher_defeated` | Cipher KO/secure (step 7) |
| `operatives_defeated = 1 → 2` | Cipher then Relay eventMappings (absolute set) |
| `attack_mechanism_known` | submit flag_4 (step 10) |
| `operative_relay_defeated` | Relay KO/secure (step 11) |
| `voltage_captured` / `voltage_escaped` | Voltage stance (step 13) |
| `voltage_neutralised` | either Voltage outcome (step 13) |
| `esd_authorized` | `voltage_neutralised` + `anomaly_detected` (step 14) |
| `attack_prevented`, `remote_trigger_disabled`, `mission_complete` | ESD press (step 15) |

## Reconciliation against the dungeon graph (Puzzle Graph)

| Graph node | Walkthrough step | Status |
|---|---|---|
| `main_entrance` (start) | Step 2 | ✅ covered |
| `npc_robert_vance` → `facility_access_keycard_level_1` | Step 4 | ✅ covered |
| `action_meet_robert_vance` → `aim_confirm_the_lie` | Step 4 | ✅ covered |
| `maintenance_work_orders` | Step 5 | ✅ covered |
| `analog_thermometer_rack_bank_c_wall` → `aim_confirm_the_lie` | Step 6 | ✅ covered |
| `npc_entropy_operative_cipher` → `workshop_keycard_level_2` → `door_engineering_workshop` | Steps 7–8 | ✅ covered |
| `bms_jump_server_terminal` → flags 1–4 → `aim_map_the_attack` | Steps 9–10 | ✅ covered |
| `npc_entropy_operative_relay` → `master_keycard` → `door_plant_room` | Steps 11–12 | ✅ covered |
| `lock_esd_pushbutton` → `aim_stop_the_runaway` | Steps 14–15 | ✅ covered |
| `facility_access_keycard_level_1` → `door_battery_hall_2` | Step 12 (Level-1 gate) | ✅ covered |
| `lock_security_equipment_locker` (spare Level 1) | Optional | ⚠️ KO-safety only, off critical path |
| `lock_extraction_gobag` → `social_fabric_safehouse_card_encoded` | Optional | ⚠️ lore/optional, off critical path |
| `h_gas_detector_panel`, `scada_hmi_main_display` | — | ⚠️ clue objects, no task node |
| `confront_voltage` (ink-driven) | Step 13 | ⚠️ no puzzle-graph node (conversation-gated, not a lock) |
| `report_to_0x99` (debrief) | Step 16 | ⚠️ no puzzle-graph node (conversation-gated) |

Every **puzzle-graph gating node** maps to a critical-path step. The three ⚠️ "no graph node" rows are conversation/interaction gates (`confront_voltage`, `report_to_0x99`) and clue objects the puzzle graph intentionally doesn't model as locks — not defects. The two optional container branches (spare keycard, go-bag) are KO-safety / lore, correctly off the critical path.

## Testing Checklist

- [ ] Opening briefing plays once; visiting the timeline topic still surfaces the Architect/Social Fabric beat before deployment.
- [ ] `enter_facility` auto-completes at reception.
- [ ] Vance hands a Level 1 keycard; `meet_robert_vance` completes; `confirm_the_lie` unlocks.
- [ ] Work orders → `find_infiltration_evidence`; thermometer → `identify_scada_anomalies` (timers start here, not before).
- [ ] Cipher drops a recoverable Level 2; workshop entry completes `locate_compromised_systems`.
- [ ] All four flags submit and complete; flag_4 sets `attack_mechanism_known`; `map_the_attack` unlocks.
- [ ] Relay drops a recoverable Master; plant room entry works.
- [ ] All three Voltage stances complete `confront_voltage` and set `voltage_neutralised`.
- [ ] ESD refuses without `esd_authorized`; press sets `mission_complete`; timers cancel.
- [ ] Debrief plays; `report_to_0x99` completes at the end; `bond_visualiser` appears; credits title matches vent state.

### Edge Cases

- **Timer path (COSTLY SUCCESS):** `anomaly_detected` starts T+22m `h2_advisory` (`hydrogen_alarm`, `urgency_stage 3`) and T+40m `racks_vent` (`racks_vented`, `casualties_occurred`, `urgency_stage 4`). The run **does not end** — the player can still reach the ESD; debrief branches on `racks_vented`.
- **KO everyone:** Vance (spare Level 1), Cipher, Relay all have `taskOnKO` + recoverable drops; Voltage KO sets `voltage_captured`; a KO'd Vance is acknowledged via `robert_vance_ko` in the debrief.
- **Out-of-order KO:** `operatives_defeated` is set absolutely (1 then 2); Voltage's line now reads "put two of my people down" so it can't mis-name on a Relay-before-Cipher order.
- **Both endings** satisfy `requiresCompleted: [identify_scada_anomalies, confront_voltage, disable_attack_vectors]`.

## Development Status

| Minigame | Status |
|---|---|
| person-chat (6 NPCs) | ✅ Implemented |
| phone-chat (HaX + Vance hubs) | ✅ Implemented |
| VM launcher + flag-station | ✅ Implemented, ⚠️ flags unverified (no SecGen build) |
| ESD pushbutton | ✅ Implemented |

Manual test consoles: set `esd_authorized`, `anomaly_detected`, `voltage_neutralised` via the debug global setter to jump to the ESD; set `racks_vented` to preview the COSTLY SUCCESS debrief branch.

---

Run `/scenario-design-review` for full objectives scaffolding analysis.
