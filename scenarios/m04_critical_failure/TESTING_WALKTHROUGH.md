# Critical Failure (m04_critical_failure) — Testing Walkthrough

> Auto-generated by the walkthrough-scenario skill. Last updated: 2026-06-12.
> Reconciled against: dungeon_graph.md (last run: 2026-06-12).
> Setting: Albion Energy Storage — 200 MWh grid-scale lithium-ion BESS.

## Prerequisites
- Validator passes: `ruby scripts/validate_scenario.rb scenarios/m04_critical_failure/scenario.json.erb` → schema validation passed, objective task wiring OK. Only good-practice/suggestion notices (15), no ❌ INVALID / ⚠️ blocking WARNINGs.
- All Ink files compiled (`ink/*.json` present for: opening_briefing, phone_agent0x99, phone_robert_chen, closing_debrief, npc_robert_chen, npc_operative_cipher/relay/static, npc_voltage, npc_security_guard).
- Note: the two SCADA/BMS terminal displays are minigame/display objects, NOT Ink — by design.
- Minigames / interactive objects:
  - VM challenge `vulnerability_analysis` (BMS Jump Server) + flag-station drop-site — ✅ Implemented
  - `esd_button` emergency-shutdown minigame (ESD pushbutton) — ✅ Implemented
  - `thermometer` analog instrument (triggerOnInteract) — ✅ Implemented
  - PIN locks: security_equipment_locker, extraction_gobag — ✅ Implemented (lockpick / keyPins)

---

## Aim 1: Infiltrate Facility and Confirm Threat
[Unlocks at start — `infiltrate_facility` active]

### Step 1 — Reception (main_entrance)
Player spawns. Opening briefing cutscene plays (`opening_briefing_cutscene`, sets `briefing_played`); Agent 0x99 sends the threat brief over the phone. → task `enter_facility` complete (enter_room: main_entrance). Security guard present (optional combat, `security_guard_ko`).

### Step 2 — Operations Office → Meet Robert Chen
Walk south into operations_office. Talk to **Robert Chen** (knot `initial_meeting`). → `#complete_task:meet_robert_chen` fires on the first line. Chen holds **Facility Access Keycard (Level 1)** (`facility_keycard_level1`) in `itemsHeld` — take it from him. (Trust/ally branches set `chen_is_ally`, which enables the Chen phone line; narrative only, not gating.)

### Step 3 — Operations Office → Read Maintenance Work Orders
Pick up **Maintenance Work Orders** → `onPickup` sets `evidence_maintenance_logs_found: true`. Agent 0x99 phone has an eventMapping on `global_variable_changed:evidence_maintenance_logs_found` → completes task `find_infiltration_evidence` and messages "that work order was never authorised."

### Step 4 — SCADA Control Room (context, optional reads)
Walk east into scada_control_room. Read the **SCADA HMI Main Display** (shows falsified 28 °C / NORMAL) and the parameter change log. No task completion here — this is the falsified-telemetry clue that motivates Step 5. (No keycard needed; room is open.)

### Step 5 — Battery Hall 1 → Read the Analog Thermometer
Walk south from SCADA control room into battery_hall_1. Interact with the **Analog Thermometer — Rack Bank C Wall** (reads 61 °C vs SCADA's 28 °C). `triggerOnInteract` → sets `anomaly_detected: true` AND completes task `identify_scada_anomalies`. **This `anomaly_detected` flag is the auth gate for the ESD button (Step 12).**

→ Aim 1 complete. Aim 2 `investigate_scada_compromise` unlocks.

---

## Aim 2: Investigate SCADA / BMS Compromise
[Unlocks after Aim 1 / Chen access]

### Step 6 — Battery Hall 1 → Defeat ENTROPY Operative 'Cipher'
Cipher is hostile (chaseSpeed 110). Knock out. → `taskOnKO: neutralize_operative_cipher` completes; `globalVarOnKO: operative_cipher_defeated`. Cipher drops **Workshop Keycard (Level 2)** (`server_room_keycard`) — take it. (Conversation also carries `#complete_task:neutralize_operative_cipher` as a backup.)

### Step 7 — Engineering Workshop (locked: RFID `server_room_keycard`)
With the Level 2 keycard, enter engineering_workshop (from SCADA control room or battery hall 1). → task `locate_compromised_systems` complete (enter_room). Agent 0x99 messages about the BMS jump server. The SAFETYNET Tasking Note clarifies: the four flag submissions are required; the three recon `custom` tasks are optional methodology.

### Step 8 — Engineering Workshop → BMS Jump Server VM + Submit 4 Flags
Launch the **BMS Jump Server Terminal** VM (`vulnerability_analysis`, 192.168.100.10). Solve and collect the four flags, then submit at the **SAFETYNET Drop-Site Terminal** (flag-station, `acceptsVms: vulnerability_analysis`):
- `flag{network_scan_complete}` → task `submit_network_scan_flag`
- `flag{ftp_intel_gathered}` → task `submit_ftp_intel_flag`
- `flag{http_analysis_complete}` → task `submit_http_analysis_flag`
- `flag{distcc_exploit_complete}` → task `submit_distcc_exploit_flag`

On `submit_distcc_exploit_flag`, Agent 0x99 messages: attack mechanism confirmed, get to the plant room ESD.
(Optional `custom` tasks — `scan_scada_network`, `investigate_compromised_services`, `exploit_distcc_vulnerability` — are `optional: true` methodology and not on the critical path. [TRIGGER UNKNOWN — optional, expected] no in-world completer; intentional.)

### Step 9 — Battery Hall 2 / Inverter Room (locked: RFID `facility_keycard_level1`)
Use the Level 1 keycard (from Chen, Step 2) to enter battery_hall_2 (east of battery hall 1). **Defeat ENTROPY Operative 'Relay'** (hostile, patrolling, chaseSpeed 120). → `taskOnKO: neutralize_operative_relay`; `globalVarOnKO: operative_relay_defeated`. Relay drops the **Master Keycard** (`master_keycard`) — take it.

→ Aim 2 complete (Cipher + Relay neutralised, flags submitted).

---

## Aim 3: Neutralise Attack Threat
[Unlocks after Aim 2; mission conclusion — `requiresCompleted: [disable_attack_vectors]`]

### Step 10 — Plant Room (locked: RFID `master_keycard`)
Use the Master Keycard to enter plant_room (west of battery hall 1). Voltage (Critical Mass Leader, hostile chaseSpeed 145) and Operative Static (hostile chaseSpeed 130) are present.

### Step 11 — Confront / Defeat Voltage (+ Static)
Defeat Voltage. → `taskOnKO: confront_voltage`; `globalVarOnKO: voltage_captured`. (Voltage's Ink also fires `#complete_task:confront_voltage` on its captured/escaped branches; `voltage_escaped` is the alternate outcome.) Defeat Static → `operative_static_defeated`; Static drops the Critical Mass Coordination Log.

### Step 12 — Plant Room → Press the Emergency Shutdown Pushbutton
Interact with the **ESD Pushbutton — Rack Banks A-C** (emergency-button, `esd_button` minigame). Gated by `authVar: anomaly_detected` (set in Step 5 — if not set, it refuses and tells you to read the analog thermometer). Confirm shutdown. `completionActions`:
- set `attack_prevented: true`
- set `remote_trigger_disabled: true`
- set `mission_complete: true`
- complete task `disable_attack_vectors`

→ Music switches to spy-action; thermometer/H₂ observation variants flip to "falling."

### Step 13 — Debrief (Agent 0x99 phone)
On `global_variable_changed:mission_complete`, the `agent_0x99_debrief` phone NPC opens (`conversationMode: phone-chat`, knot `start`), and a timed message prompts "tap to open your debrief." Opening it fires `#complete_task:report_to_0x99` at the top of the debrief knot.

### Step 14 — Mission Conclusion (WIN)
Aim `neutralize_attack_threat` has `missionConclusion: true` and `requiresCompleted: [disable_attack_vectors]` (done Step 12). With `confront_voltage` + `disable_attack_vectors` + `report_to_0x99` complete, the aim closes → `bond_visualiser` conclusion screen. Closing the debrief (`conversation_closed:agent_0x99_debrief`) triggers the victory playlist + credits roll (MISSION COMPLETE / thermal runaway averted / grid held). **Win condition reached.**

---

## Global Variable State (end of critical path)

| Variable | Value | Trigger source |
|---|---|---|
| `briefing_played` | true | opening cutscene (Step 1) |
| `evidence_maintenance_logs_found` | true | pick up Work Orders (Step 3) |
| `chen_is_ally` | true | Chen ally branch (Step 2, narrative) |
| `anomaly_detected` | true | read analog thermometer (Step 5) — **ESD auth gate** |
| `operative_cipher_defeated` | true | KO Cipher (Step 6) |
| `flag_*_submitted` (×4) | true | drop-site flag submissions (Step 8) |
| `operative_relay_defeated` | true | KO Relay (Step 9) |
| `voltage_captured` | true | KO/confront Voltage (Step 11) |
| `operative_static_defeated` | true | KO Static (Step 11) |
| `attack_prevented` | true | ESD button (Step 12) |
| `remote_trigger_disabled` | true | ESD button (Step 12) |
| `mission_complete` | true | ESD button (Step 12) |
| `lore_cross_cell_coordination_found` | true | pick up Coordination Log (Step 11) |

---

## Reconciliation against Puzzle Graph

| Graph node | Walkthrough step | Status |
|---|---|---|
| `main_entrance` → `operations_office` | Steps 1–3 | ✅ covered |
| `maintenance_work_orders` | Step 3 | ✅ covered |
| `npc_robert_chen` | Step 2 | ✅ covered |
| `facility_access_keycard_level_1` | Step 2 (taken from Chen) | ✅ covered |
| `action_meet_robert_chen` | Step 2 | ✅ covered |
| `scada_hmi_main_display` | Step 4 | ✅ covered (clue) |
| `analog_thermometer_rack_bank_c_wall` | Step 5 | ✅ covered (sets `anomaly_detected`) |
| `npc_entropy_operative_cipher` | Step 6 | ✅ covered |
| `workshop_keycard_level_2` | Step 6 (drop) | ✅ covered |
| `door_engineering_workshop` | Step 7 | ✅ covered |
| `bms_jump_server_terminal` (VM) | Step 8 | ✅ covered |
| `vmch/vmfl_*` (4 flags) | Step 8 | ✅ covered |
| `door_battery_hall_2` | Step 9 (L1 keycard) | ✅ covered |
| `npc_entropy_operative_relay` | Step 9 | ✅ covered |
| `master_keycard` | Step 9 (drop) | ✅ covered |
| `door_plant_room` | Step 10 (master keycard) | ✅ covered |
| `lock_esd_pushbutton` | Step 12 | ✅ covered (climax) |
| `npc_voltage` (graph: in plant room) | Step 11 | ✅ covered |
| `lock_pick_kit` → `security_equipment_locker` | Optional | ⚠️ side content (spare L1 keycard backup) |
| `loading_dock` / `extraction_gobag` / `social_fabric_safehouse_card_encoded` | Optional | ⚠️ side content (lore, post-climax) |
| `h_gas_detector_panel` | — | ⚠️ clue node, no task (read-only flavour) |

No ❌ uncovered mandatory nodes. The ⚠️ items are intentionally optional / read-only clue content, not critical-path gaps.

---

## Testing Checklist
- [ ] Opening cutscene plays once; `briefing_played` set; `enter_facility` complete.
- [ ] Meet Chen → `meet_robert_chen` complete; take Level 1 keycard.
- [ ] Pick up Maintenance Work Orders → `find_infiltration_evidence` complete (via 0x99 phone mapping).
- [ ] Read analog thermometer → `anomaly_detected` set + `identify_scada_anomalies` complete; Aim 2 unlocks.
- [ ] KO Cipher → `neutralize_operative_cipher`; pick up Level 2 keycard.
- [ ] Enter workshop → `locate_compromised_systems` complete.
- [ ] Submit 4 VM flags → 4 `submit_*_flag` tasks complete.
- [ ] Enter Battery Hall 2 (L1 keycard); KO Relay → `neutralize_operative_relay`; take Master Keycard.
- [ ] Enter Plant Room (master keycard); defeat Voltage → `confront_voltage`; defeat Static.
- [ ] Press ESD button (auth = `anomaly_detected`) → `attack_prevented` + `mission_complete` + `disable_attack_vectors`.
- [ ] Debrief opens → `report_to_0x99` complete; conclusion screen + victory credits.

### Optional Path
- Security Office: lockpick the equipment locker (keyPins 30/45/35/50) for a spare Level 1 keycard + incident guide.
- Loading Dock (post-plant-room): lockpick the extraction go-bag (keyPins 40/30/50/35) → encoded Social Fabric safehouse card (Base64 lore lead).
- Recon `custom` tasks (scan/investigate/exploit) are `optional: true` — no in-world completer (intentional methodology checkboxes).

### Edge Cases
- **ESD pressed before reading thermometer**: blocked by `authVar: anomaly_detected`; shows `unauthorizedText` directing player to the thermometer. Good — no dead zone.
- **Voltage escaped branch**: `voltage_escaped` set instead of `voltage_captured`; `confront_voltage` still completes via Ink — mission still completable.
- **No timer**: urgency is stage-based (`urgency_stage`), so there is no fail-by-time hard lock.

---

## Development Status

| Object / minigame | Type | Status |
|---|---|---|
| BMS Jump Server | vm-launcher (`vulnerability_analysis`) | ✅ Implemented |
| SAFETYNET Drop-Site | flag-station (4 flags) | ✅ Implemented |
| ESD Pushbutton | emergency-button (`esd_button`) | ✅ Implemented |
| Analog Thermometer | thermometer (triggerOnInteract) | ✅ Implemented |
| Security locker / go-bag | safe / suitcase PIN locks | ✅ Implemented |

Manual-testing console helpers (set state directly):
- `globalVars.anomaly_detected = true` — unlock the ESD button without the thermometer.
- `globalVars.mission_complete = true` — force the debrief phone to open.

Summary: 14 critical-path steps. No genuine [TRIGGER UNKNOWN] or [DEAD ZONE RISK] on the mandatory path (the only TRIGGER-UNKNOWN items are the three intentionally optional recon tasks). Reconciliation is clean against the Puzzle Graph; all ⚠️ items are optional/clue content. The scenario is completable end-to-end.

*Run `/scenario-design-review` for full objectives scaffolding analysis.*
