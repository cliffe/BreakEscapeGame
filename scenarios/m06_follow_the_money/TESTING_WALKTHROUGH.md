# Follow the Money — Testing Walkthrough

> Auto-generated by the walkthrough-scenario skill. Last updated: 2026-08-28.
> Reconciled against: dungeon_graph.md (last run: 2026-08-28).

Critical path (6 aims, 5 hops): **Establish Cover → Get Into The Back Office → Crack The Backend → Map The Money → Reach The Top Floor → Settle The Account**

## Prerequisites
- Validator passes: schema ✅, ink ✅, objective wiring ✅, room geometry ✅ (0 errors, 0 warnings)
- All 7 Ink files compiled (`bash scripts/compile-ink.sh m06_follow_the_money`)
- SecGen VMs built from `SecGen/scenarios/break_escape/safetynet/m06_follow_the_money.xml` (flag key `hackme_crack_me_lab`, console `kali_cracker`) — see `VM_INTEGRATION.md`
- Minigames used: password door lock ✅, RFID lock/cloner ✅, PIN safe ✅, notes/text-file reader ✅, vm-launcher + flag-station ✅

## Aim: Establish Cover
[Unlocks at start]

1. **Reception Lobby (opening cutscene)** — Agent HaX briefing auto-plays on `game_loaded` (`timedConversation`, `skipIfGlobal: briefing_played`) → `#complete_task:receive_briefing`, `briefing_played` set; briefing will not replay on resume.
2. **Trading Floor — Dr. Elena Volkov** — Open any dialogue branch with her → `meet_elena` completes → `onComplete: unlockAim access_backend_systems`; handler sets `exchange_infiltrated`.

## Aim: Get Into The Back Office
[Unlocks after: aim `establish_cover` complete]

3. **Trading Floor — Elena** — Choose "I need system access" hub option (`request_passwords`, gated `elena_trust >= 15`) → `#give_item:text_file:m06_password_dictionary`, `#complete_task:obtain_access_tools`, `found_password_lists` set. Handler offers the cracking field guide.
4. **Security Checkpoint — IT New Starter Checklist** — Read the note → `server_passphrase_known` set; handler completes `find_server_credentials` and barks the passphrase (`bitcoin` + current year → `bitcoin2025`).
5. **Server Room door** — Enter passphrase `bitcoin2025` at the password lock → door opens.
6. **Server Room (enter)** — `access_server_room` completes → `onComplete: unlockAim crack_passwords`.
7. *(Optional)* **Trading Floor — Dani Okonkwo (trader)** — talk → `question_the_trader` (optional; `taskOnKO` fallback present).

## Aim: Crack The Backend
[Unlocks after: aim `access_backend_systems` complete]

8. **Server Room — HashChain Backend Server Terminal** — Launch the vm-launcher (console attaches to `kali_cracker`) → `access_hackme_vm` completes; handler offers the reconnaissance guide.
9. **Kali console** — Foothold on `hackme_crack_me_lab` (distcc RCE → readable shadow → crack `hcauth`). Submit **flag_1** at the flag-station → `submit_flag1` completes → handler `setGlobal flag1_submitted`, `servers_accessed=1`; offers privilege-escalation guide. Puzzle graph unlocks `credential_reuse_available`.
10. **Kali console** — Reuse `hcauth`'s password on `hcledger`. Submit **flag_2** → `submit_flag2` completes → `flag2_submitted`, `servers_accessed=2`.
11. **Kali console** — Crack `hcfindb` (financial database). Submit **flag_3** → `submit_flag3` completes → `flag3_submitted`, `servers_accessed=3`.
12. **Kali console** — Escalate via `hcops` sudo to read `hcvault`. Submit **flag_4** → `submit_flag4` completes → `flag4_submitted`, `servers_accessed=4` → `onComplete: unlockAim map_financial_network`; handler points to the blockchain lab.

## Aim: Map The Money
[Unlocks after: aim `crack_passwords` complete]

13. **Blockchain Lab (enter)** — `access_blockchain_lab` completes.
14. *(Optional)* **Blockchain Lab — Priya Raghavan (analyst)** — talk → `question_the_analyst` (optional; `taskOnKO` fallback present).
15. **Blockchain Lab — ENTROPY Transaction Network Analysis** — Read/take the note → `onPickup: found_blockchain_evidence, financial_network_mapped` set; handler mapping completes `find_transaction_records` and sets `blockchain_debrief_available`.
16. *(Optional)* **Data Centre — Financial Transaction Server (PC)** — read the 3 files (`transaction_server_files` group) → `read_transaction_server_files` progresses (optional).
17. **Data Centre (enter)** — `access_data_center` completes.
18. **Data Centre — The Architect's Fund Allocation** — Read/take → `onPickup: found_architects_fund, knows_coordinated_attack`; handler completes `discover_architects_fund`, sets `knows_architects_fund` → `onComplete: unlockAim breach_executive_wing`. Music shifts to spy-action. Satoshi is revealed (`setVisible` on `found_architects_fund`).
19. **Data Centre — Wallet Recovery Keys** — Read/take → `found_wallet_keys` set; handler flags the seize-vs-monitor choice ahead.

## Aim: Reach The Top Floor
[Unlocks after: aim `map_financial_network` complete]

20. **Data Centre — Executive Access Badge** — Take the badge → `take_executive_badge` completes (collect_items); puzzle graph unlocks `executive_wing`.
21. **Executive Wing door** — Present `executive_badge` at the RFID lock → door opens.
22. **Satoshi's Office (enter)** — `access_satoshi_office` completes → `onComplete: unlockAim resolve_the_fund`.
23. *(Optional)* **Elena's Office** (via CTO badge, step-3-adjacent) & **Executive Safe** — read the Architect email / manifesto margin note (`executive_safe_pin` clue = `2140`), open the safe → `open_executive_safe` (optional) → `architect_identity_found`.

## Aim: Settle The Account  *(missionConclusion → bond_visualiser)*
[Unlocks after: aim `breach_executive_wing` complete]
Requires completed: meet_elena, access_server_room, submit_flag1, submit_flag4, find_transaction_records, discover_architects_fund, **confront_satoshi, decide_asset_strategy, decide_elena_fate**

24. **Satoshi's Office — Satoshi Nakamoto II** — Confront him. Terminal branch → `#complete_task:confront_satoshi`, `satoshi_confronted` set. *(If the player has shown no evidence, `arrest_resisted` can turn him `#hostile` — combat path; KO sets `satoshi_ko` → handler completes `decide_asset_strategy` + `assets_seized`.)*
25. **Satoshi's Office — asset choice** — Choose **Seize** (`seize_assets` → `assets_seized`, `assets_decided`) or **Monitor** (`enable_monitoring` → `monitoring_enabled`, `assets_decided`) → `#complete_task:decide_asset_strategy`.
26. **Trading Floor — Elena (or her KO)** — Resolve her fate: **Recruit** (`recruitment_finalized` → `elena_recruited`) or **Arrest** (`arrest_elena` → `elena_arrested`), each sets `elena_fate_decided` + `#complete_task:decide_elena_fate`. *(KO alternative: `elena_ko` → handler completes `decide_elena_fate`.)*
27. **WIN CONDITION** — When both `decide_asset_strategy` and `decide_elena_fate` are complete (either order), handler sets `mission_complete` + `start_debrief_cutscene` → hidden `closing_debrief_person` fires (`conversationMode: person-chat`, `disableClose`) → conclusion aim completes → `bond_visualiser` + credits scroll.

## Global Variable State (end of critical path)

| Variable | Set by | Step |
|---|---|---|
| `briefing_played` | opening `timedConversation` | 1 |
| `exchange_infiltrated` | handler on `meet_elena` | 2 |
| `found_password_lists` | Elena `request_passwords` | 3 |
| `server_passphrase_known` | IT checklist `onPickup` | 4 |
| `flag1_submitted` … `flag4_submitted` | handler on `submit_flagN` | 9–12 |
| `servers_accessed` (→4) | handler on each flag | 9–12 |
| `found_blockchain_evidence`, `financial_network_mapped` | blockchain note `onPickup` | 15 |
| `found_architects_fund`, `knows_coordinated_attack`, `knows_architects_fund` | fund doc `onPickup` + handler | 18 |
| `satoshi_revealed` | `setVisible` mapping on `found_architects_fund` | 18 |
| `found_wallet_keys` | wallet keys `onPickup` | 19 |
| `architect_identity_found` | safe contents `onPickup` (optional) | 23 |
| `satoshi_confronted` | Satoshi terminal branch | 24 |
| `assets_seized` **or** `monitoring_enabled`, `assets_decided`, `final_choice` | asset choice | 25 |
| `elena_recruited` **or** `elena_arrested`, `elena_fate_decided` | Elena fate | 26 |
| `mission_complete`, `start_debrief_cutscene` | handler when both decisions done | 27 |

## Testing Checklist

- [ ] Opening briefing auto-plays once, does not replay on resume
- [ ] `meet_elena` unlocks aim 2
- [ ] Password dictionary transfers to inventory; `obtain_access_tools` completes
- [ ] `bitcoin2025` opens the server room door
- [ ] Entering server room unlocks aim 3
- [ ] All four flags submit and complete their tasks in order; `flagN_submitted` globals set
- [ ] flag_4 unlocks aim 4
- [ ] Blockchain note sets `financial_network_mapped`, completes `find_transaction_records`
- [ ] Fund doc completes `discover_architects_fund`, unlocks aim 5, reveals Satoshi, shifts music
- [ ] Executive badge opens the executive wing; entering Satoshi's office unlocks aim 6
- [ ] Confront Satoshi completes `confront_satoshi`
- [ ] Asset choice completes `decide_asset_strategy`
- [ ] Elena fate completes `decide_elena_fate`
- [ ] Both decisions → `mission_complete` + debrief cutscene + bond_visualiser + credits

### Optional Path
- [ ] Trader (`question_the_trader`) and analyst (`question_the_analyst`) optional conversations
- [ ] Transaction-server files (`read_transaction_server_files`, group of 3)
- [ ] Elena's office (CTO badge or RFID clone) → Architect email → safe PIN `2140` → `open_executive_safe` → Architect identity file
- [ ] RFID cloner as alternate route to the CTO badge (vs. Elena handing it over)

### Edge Cases
- [ ] **KO any NPC**: `elena_ko` / `satoshi_ko` / `trader_ko` / `analyst_ko` set via `globalVarOnKO`; `taskOnKO` fallbacks complete the gated task; credits branch on each. Satoshi KO also auto-resolves the asset decision (seize by force).
- [ ] **Satoshi resists arrest** (no evidence shown) → `#hostile` combat branch; mission still resolvable via KO handler mapping.
- [ ] **Skip the safe**: debrief acknowledges missing Architect identity rather than asserting it (`architect_identity_found` conditional).
- [ ] **Decisions in either order**: both handler mappings guard on the other decision being done, so the win fires regardless of sequence.

## Development Status

| Element | Status |
|---|---|
| Password door lock (server room) | ✅ Implemented |
| RFID lock + cloner (elena_office, executive_wing) | ✅ Implemented |
| PIN safe (executive_safe) | ✅ Implemented |
| vm-launcher + flag-station (4 flags) | ✅ Implemented |
| Notes / text-file reader | ✅ Implemented |
| Field guides (4, via Agent HaX hub) | ✅ Implemented |
| bond_visualiser conclusion + credits | ✅ Implemented |

Manual console checks:
```
ruby scripts/validate_scenario.rb scenarios/m06_follow_the_money/scenario.json.erb
bash scripts/compile-ink.sh m06_follow_the_money
python3 scripts/predict_door_sides.py scenarios/m06_follow_the_money/scenario.json.erb
```

## Reconciliation against Puzzle Graph

Clean except one benign orphan task (below).

| Graph node (Puzzle Graph) | Walkthrough step | Status |
|---|---|---|
| `it_new_starter_checklist → door_server_room` | Step 4–5 | ✅ covered |
| `npc_dr_elena_volkov → password_dictionary_list → lock_vm_launcher_hackme` | Step 3, 8 | ✅ covered |
| `npc_dr_elena_volkov → cto_access_badge → door_elena_office` | Step 23 (optional) | ✅ covered |
| `rfid_badge_cloner ⇢ door_elena_office` | Optional path | ✅ covered |
| `hashchain_backend_server_terminal → vmch_submit_flag1` | Step 9 | ✅ covered |
| `vmfl_submit_flag1 → …flag2 → …flag3 → …flag4 → blockchain_lab` | Steps 9–13 | ✅ covered |
| `entropy_transaction_network_analysis → lock_architects_fund_doc` | Step 15, 18 | ✅ covered |
| `the_architect_s_fund_allocation → lock_executive_access_badge` | Step 18, 20 | ✅ covered |
| `executive_access_badge → door_executive_wing` | Step 20–21 | ✅ covered |
| `email_from_the_architect / manifesto → lock_executive_safe` | Step 23 (optional) | ✅ covered |
| `npc_satoshi_nakamoto_ii → action_` (confront) | Step 24 | ✅ covered |
| `find_server_credentials` (manual task) | Step 4 | ✅ covered — handler completes it on `server_passphrase_known` (IT checklist read) |

No orphan walkthrough steps (every critical action maps to a graph node). All VM flag nodes, both RFID paths, and the fund/badge chain are represented.

## Summary

The critical path is **27 numbered steps across 6 aims** and is completable end-to-end: every required task has an in-world trigger, aim hand-offs are explicit (`onComplete: unlockAim` on the last critical task of each aim), and the win condition fires deterministically once both terminal decisions are made — in either order — via the handler's cross-guarded mappings. The previously orphaned `find_server_credentials` manual task is now completed by a handler mapping on `global_variable_changed:server_passphrase_known` (fired when the player reads the IT checklist), with a confirming bark that names the passphrase. No **[TRIGGER UNKNOWN]** and no **[DEAD ZONE RISK]** remain. Reconciliation against the Puzzle Graph is clean with no orphan steps.

Run `/scenario-design-review` for full objectives scaffolding analysis.
