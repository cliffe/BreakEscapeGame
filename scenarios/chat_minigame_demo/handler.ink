// ================================================
// chat_minigame_demo: Handler (phone-chat, LCD theme)
// Opened from the "Handler Phone" in inventory. Demonstrates the default
// phone-chat texting UI: message bubbles, choices, and a timed push message.
// No #speaker tags needed -- the phone UI attributes lines to the contact.
// ================================================

=== start ===
This is the phone-chat view -- a texting thread. No portraits, just messages.
+ [Neat. What else can it do?]
    It handles choices, delays, and timed pushes -- like the one you just got.
    The other device you're carrying uses the terminal theme: same engine, different skin.
    #exit_conversation
    -> DONE
+ [Got it.]
    Good. Poke the terminal phone next, then read the video console.
    #exit_conversation
    -> DONE
